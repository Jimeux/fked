package slack

// Request types
const (
	EventCallbackRequestType   = "event_callback"
	URLVerificationRequestType = "url_verification"
)

// Callback event types
const (
	AppMentionEvent = "app_mention"
)
